# Increase pnpm's resilience to network issues
fetch-retries=5
fetch-retry-maxtimeout=120000
fetch-timeout=180000

# Reduce concurrent connections to avoid overwhelming the network
network-concurrency=5 
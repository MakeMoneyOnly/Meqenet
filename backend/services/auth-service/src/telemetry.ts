// Telemetry configuration placeholder
// OpenTelemetry setup moved to shared/observability/otel.ts

export const telemetryConfig = {
  serviceName: 'auth-service',
  enabled: true,
};

export default telemetryConfig;
